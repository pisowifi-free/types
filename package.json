{
  "name": "@pisowifi-free/types",
  "version": "1.0.0",
  "description": "This package contains type definitions for Piso Wifi Free environment.",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc -w",
    "build": "tsc",
    "deploy": "npm run build && npm publish --access=public"
  },
  "author": "Coolay",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.5.2"
  },
  "types": "./dist\\index.d.ts"
}
